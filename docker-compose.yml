version: '2.1'
services:
  bot:
    build: .
    restart: "unless-stopped"
    environment:
      TG_API_KEY:
      URL_PATH:
      URL_HOSTNAME:
      URL_PROXY_HOST:
      MONGO_HOST: "mongo"
      MONGO_USER: "mongo"
      MONGO_PASSWORD: "1234secure"
      MONGO_DB: "rules_rules"
      HEALTHCHECK_URL: "/healthcheck"